A decorator is a design pattern in Python that allow a user to add new functionality 
to an existing object without modifying its structure.

Decorator are usually called before the definition of a function u want to
decorate 